//! IntegrationEvent REST handlers
//!
//! Auto-generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{IntegrationEventService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateIntegrationEventDto, UpdateIntegrationEventDto, PatchIntegrationEventDto, IntegrationEventResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum IntegrationEventError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for IntegrationEventError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for IntegrationEventError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "INTEGRATIONEVENT_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "INTEGRATIONEVENT_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "INTEGRATIONEVENT_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "INTEGRATIONEVENT_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for IntegrationEvent.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /integration_events | List with pagination |
/// | POST | /integration_events | Create new |
/// | GET | /integration_events/:id | Get by ID |
/// | PUT | /integration_events/:id | Full update |
/// | PATCH | /integration_events/:id | Partial update |
/// | DELETE | /integration_events/:id | Soft delete |
/// | POST | /integration_events/bulk | Bulk create |
/// | POST | /integration_events/upsert | Upsert |
/// | GET | /integration_events/trash | List deleted |
/// | POST | /integration_events/:id/restore | Restore |
/// | DELETE | /integration_events/empty | Empty trash |
/// | GET | /integration_events/:id/deleted | Get deleted by ID |
/// | DELETE | /integration_events/trash/:id | Permanent delete from trash |
/// | GET | /integration_events/count | Count active entities |
/// | GET | /integration_events/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(IntegrationEventService::with_repository(repository));
/// let router = create_integration_event_routes(service);
/// ```
pub fn create_integration_event_routes(service: Arc<IntegrationEventService>) -> Router {
    BackboneCrudHandler::<IntegrationEventService, IntegrationEvent, CreateIntegrationEventDto, UpdateIntegrationEventDto, IntegrationEventResponseDto>::routes(
        service,
        "/integration_events",
    )
}

/// Create Axum router with only the read (GET) endpoints for IntegrationEvent.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_integration_event_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_integration_event_read_routes(service: Arc<IntegrationEventService>) -> Router {
    BackboneCrudHandler::<IntegrationEventService, IntegrationEvent, CreateIntegrationEventDto, UpdateIntegrationEventDto, IntegrationEventResponseDto>::read_routes(
        service,
        "/integration_events",
    )
}

/// Create Axum router with only the write (mutation) endpoints for IntegrationEvent.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
pub fn create_integration_event_write_routes(service: Arc<IntegrationEventService>) -> Router {
    BackboneCrudHandler::<IntegrationEventService, IntegrationEvent, CreateIntegrationEventDto, UpdateIntegrationEventDto, IntegrationEventResponseDto>::write_routes(
        service,
        "/integration_events",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_integration_event_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<IntegrationEventService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_integration_event_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

